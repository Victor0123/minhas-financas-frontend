import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  root: {
    display: 'flex',
  },
  form: {
    marginLeft: 'auto',
    marginTop: 70,
  },
  submit: {
    marginTop: 10,
    height: 50,
  },
}));
